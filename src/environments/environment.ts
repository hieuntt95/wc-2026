export const environment = {
  supabase: {
    /**
     * Fill these with your Supabase Project URL and anon public key.
     *
     * The anon key is safe to ship to the browser when Row Level Security
     * policies are configured correctly.
     */
    url: '',
    anonKey: '',
  },
};
